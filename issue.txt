false positive in "_DANGEROUS_PATTERNS"
llm_router` never used in `process_user_request
BudgetManager monitors but never gates LLM calls
TaskGraph not driving `process_user_request` execution

1. Stop generating a brand-new agent class from scratch for every task — this is the single biggest fix. Right now every subtask spawns an LLM that writes fresh Python code, gets validated, and often fails ("capability has no corresponding method," 'str' object has no attribute 'get' — we saw both repeatedly). That's why things take 5-10 minutes and burn 15-20+ LLM calls per task. Swapping to a small set of solid, reused specialist roles (researcher, writer, coder, reviewer) that just get parameterized per task instead of regenerated would cut latency and cost dramatically and fix most of the reliability failures in one move.

2. A real assembly step. Sub-agents currently hand back prose; nothing integrates the pieces into one coherent deliverable. Add an explicit final step that asks for structured output (a manifest of filename→content pairs, or one well-formed document) instead of scraping code blocks out of paragraphs.

3. Better-grounded research. The web search I wired in works but is narrow (DuckDuckGo's free tier). Swapping to Serper or Tavily (same registration mechanism, just an API key) makes the research use case actually trustworthy instead of only working for exact topic names.

4. Real dependency ordering. There's already a whole task-graph/DAG system built (task_graph.py) that the main pipeline never uses — wiring it in would let multi-step goals like "design the schema, then write the queries" execute in correct order instead of as an unordered flat list.

5. Actually "runnable" output, at least for the simple case. For anything that produces a static site, add a step that writes the files and starts a local file server pointing at them, so "here's your site" means something you can click open — not full deployment infra, just the smallest version of "running."

6. Persistent context across related tasks. Right now every request starts fresh; there's a memory system but it's barely used. Worth it if you're iterating on the same project across multiple submissions.

The honest order I'd tackle these: #1 first (fixes cost, speed, and reliability all at once, and everything else gets easier once agents aren't randomly broken), then #2 (this is what actually gets you from "report" to "product"), then #3 if research quality matters to you, #4 and #5 after that.

Updated todo list

I’ve finished the code trace and will now give you a pure diagnostic report with exact hotspots, no code changes. I’m pulling one last pass of precise line references so you can patch confidently.

Searched for regex `def process_user_request|spawn_dynamic_agent\(|for spec in roster|for task in entry\["tasks"\]|task\.get\("type"\)` (`**/venture-os/agent-engine/core/orchestrator.py`), 7 results

Searched for regex `def spawn_dynamic_agent|Generating new agent|max_retries = 3|time\.sleep\(|validate_generated_agent_code\(|register_agent_type\(agent_name|def _determine_agent_type|if "type" in task` (`**/venture-os/agent-engine/core/agent_factory.py`), 10 results

Searched for regex `def _plan_agent_roster|EXACTLY 3 to 5 specialized AI agents|capabilities : list of method signatures` (`**/venture-os/agent-engine/core/meta_agent.py`), 4 results

Searched for regex `def validate_generated_agent_code|Capability '.*' has no corresponding method|is_valid = len\(errors\) == 0` (`**/venture-os/agent-engine/core/validator.py`), 15 results

No code changes were made. Here is where the issue is coming from.

**Findings (ordered by impact)**

1. Dynamic agent generation is still the default execution path
- In the main request flow, every planned roster entry is spawned via dynamic generation, not via fixed reusable roles:
orchestrator.py
- The same pattern is used in the async streaming path through _spawn_from_spec:
orchestrator.py
orchestrator.py
- This is the primary reason you see high latency and lots of LLM calls.

2. The factory explicitly generates brand-new classes with retries and sleeps
- Dynamic generation entry point:
agent_factory.py
- It can regenerate up to 3 times:
agent_factory.py
- It includes blocking retry backoff sleep:
agent_factory.py
- It dynamically imports and registers each generated class:
agent_factory.py
- This is exactly the 5-10 minute / high-call behavior you described.

3. Meta-agent prompt design pushes novelty instead of reuse
- Roster planner asks for exactly 3-5 specialized agents with custom names/capabilities:
meta_agent.py
- It asks for capability signatures and custom task typing:
meta_agent.py
meta_agent.py
- This structurally steers the system toward generating new classes instead of mapping to stable role agents.

4. Source of "str object has no attribute get" is weak schema/type guarding around LLM roster tasks
- Orchestrator assumes each task is a dict and immediately calls task.get:
orchestrator.py
- Async path does the same:
orchestrator.py
orchestrator.py
- If the LLM emits tasks as strings (or malformed entries), this will throw exactly that error.
- I did not find a strict roster schema validation step before these loops.

5. Source of "capability has no corresponding method" is validator warning-only logic
- The message is produced here:
validator.py
- But missing capability methods are warnings, not hard failures, because validity is only based on errors:
validator.py
- Result: agents can pass validation while still having capability-to-method mismatches, then fail at runtime.

**Secondary reliability risk**
- In the non-dynamic spawn path, task type is treated as agent type if present:
agent_factory.py
- If task type is generate_code instead of coding, this can select an invalid agent type (different path, but related fragility).

**Bottom line**
- Your diagnosis is correct: the issue lies in the architecture path that always routes through dynamic class generation from meta-agent roster output, plus weak task-shape validation and warning-only capability checks.  
- The central hotspots are:
orchestrator.py, - 316
agent_factory.py, - 358
meta_agent.py, - 62
validator.py. - 352